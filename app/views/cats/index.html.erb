<h2>Cats</h2>

<ul>  
  <% @cats.each do |cat |%>
    <li><%= link_to "#{cat.name}", cat_url(cat) %></li>
  <% end %>
</ul>

<%= button_to("Create rental request", new_cat_rental_request_url, method: :get) %>